import { FindInArrPipe } from './find-in-arr.pipe';

describe('FindInArrPipe', () => {
  it('create an instance', () => {
    const pipe = new FindInArrPipe();
    expect(pipe).toBeTruthy();
  });
});
